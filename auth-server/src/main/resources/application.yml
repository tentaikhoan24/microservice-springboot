eureka:
  instance:
    host: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8081
spring:
  application:
    name: auth
  datasource:
    url: jdbc:mysql://localhost:3306/microservice?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
